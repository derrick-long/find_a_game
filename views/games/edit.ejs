<div class="row h-100 align-items-center">
  <div class="col">
  </div>
  <div class="col-8 bg-light" id="add-form">

    <% if(errors.length > 0) {%>
      <% errors.forEach(function(error){ %>
         <div class= "alert alert-danger"> <%= error.text %> </div>
      <% }) %>
    <% } else { %>

    <% } %>

    <%- include ../partials/edit_form.ejs %>

  </div>

  <div class="col">
  </div>

</div>
